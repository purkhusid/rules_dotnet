load("@io_bazel_rules_dotnet//dotnet:defs.bzl", "fsharp_binary")

fsharp_binary(
    name = "paket2bazel.exe",
    srcs = [
        "paket2bazel.fs",
    ],
    deps = [
        "@core_sdk_stdlib//:libraryset",
        "@internal.paket.coret//:lib",
    ],
)
